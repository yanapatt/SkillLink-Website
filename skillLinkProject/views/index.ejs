<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Reviews</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #1a1a1a;
      color: white;
      font-family: Arial, sans-serif;
    }

    .navbar-container {
      background: black;
      padding: 15px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .navbar-left {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .navbar-left a {
      color: white;
      text-decoration: none;
    }

    h1 {
      margin: 0;
    }

    .container {
      width: 80%;
      margin: 0 auto;
      padding: 20px;
    }

    .form-container {
      background-color: #222;
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 30px;
    }

    .form-container input,
    .form-container textarea,
    .form-container select,
    .form-container button {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border-radius: 4px;
      border: 1px solid #ddd;
      box-sizing: border-box;
    }

    .task-list {
      background-color: #222;
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 20px;
    }

    .task-list ul {
      list-style-type: none;
      padding: 0;
    }

    .task-list li {
      padding: 10px;
      border-bottom: 1px solid #ddd;
      margin-bottom: 10px;
    }

    .task-list li:last-child {
      border-bottom: none;
    }

    .actions {
      text-align: center;
      margin-top: 20px;
    }

    .actions button {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .actions button:hover {
      background-color: #45a049;
    }
   
    .element{
      margin-top: 20px;
      color:white;
      text-align: center;
     
    }
    .priority-select {
      width: auto;
      display: inline-block;
    }

    .search-container {
      margin-top: 20px;
      text-align: center;
      border-radius: 20px;
      
    }
   
    .search-container input {
      width: 60%;
            padding: 10px;
            border-radius: 25px;
            
    }

    .search-container button {
      padding: 10px 20px;
      background-color: #000000;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .search-container button:hover {
      background-color: #ffffff;
    }

    
    .post-box {
      background: #222;
      padding: 20px;
      margin: 20px;
      border-radius: 10px;
      text-align: center;
    }

    .task-list .task-item {
      background: #333;
      padding: 15px;
      border-radius: 10px;
      margin: 10px 0;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .task-item img {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      margin-right: 15px;
    }
    .btn{
      background-color: black;
      color: white;
      border: white;
      border-radius: 20px;
    }
    .btn2{
      background-color: white;
      color: black;
      border: white;
      border-radius: 20px;
      padding: 10px 20px;
    }
  </style>
</head>

<body>


<!--navbar-->
  <div class="navbar-container">
    <div class="navbar-left">
      <a href="#">Skillink</a>
      <a href="#">หน้าหลัก</a>
      <a href="#">โพสต์</a>
    </div>
    <button class="btn ">เข้าสู่ระบบ</button>
  </div>


 <!-- Search Task Form -->
  <h1 class="element">ค้นหาอาสาสมัครที่คุณต้องการได้เลย!</h1>

 <div class="search-container">
  <form  action="/search" method="POST">
    <input class="search-container " type="text" name="searchName" placeholder="Search Task by Name" />
    <button type="submit">Search</button>
  </form>
</div>
  <div class="container">


    <!-- Add Task Form -->
    <div class="form-container">
      <form action="/add" method="POST" enctype="multipart/form-data">
        <input type="text" name="username" placeholder="User Name" required />
        <input type="text" name="name" placeholder="Task Name" required />
        <textarea name="description" placeholder="Task Description" required></textarea>
        <label for="priority">Priority:</label>
        <select name="priority" id="priority">
          <option value="1">1</option>
          <option value="2">2</option>
          <option value="3">3</option>
          <option value="4">4</option>
          <option value="5">5</option>
        </select>
        <input type="file" name="image" accept="image/*" />
        <button type="submit">Add Task</button>
      </form>
    </div>

    <h2 class="element">Task Summary by Priority</h2>
    <ul>
      <% Object.keys(summary).forEach(priority => { %>
        <li>Priority <%= priority %>: <%= summary[priority] %> tasks</li>
      <% }) %>
    </ul>

    

    <!-- Sort Button -->
    <div class="actions">
      <a href="/sort">
        <button>Sort by Priority</button>
      </a>
    </div>

    <!-- Task List with Delete Options -->
    <div class="task-list">
      <form action="/delete" method="POST">
        <ul>
          <% tasks.forEach(task => { %>
            <li class="task-item">
              <input type="checkbox" name="taskNames" value="<%= task.name %>" />
              <strong><%= task.name %></strong>: <%= task.description %> by <%= task.username %> (Priority: <%= task.priority %>)
              <a href="/view/<%= task.name %>">View</a>
            </li>
          <% }) %>
        </ul>

        <!-- Delete Selected Tasks Button -->
        <button type="submit">Delete Selected Task</button>

        <!-- Dropdown for Priority Removal -->
        <label for="prioritySelect">Remove by Priority:</label>
        <select name="priority" id="prioritySelect" class="priority-select">
          <option value="1">1 (Highest Priority)</option>
          <option value="2">2</option>
          <option value="3">3</option>
          <option value="4">4</option>
          <option value="5">5 (Lowest Priority)</option>
        </select>

        <button type="submit" name="action" value="removeByPriority">Remove by Priority</button>
      </form>

      <!-- Remove Oldest Task Button -->
      <form action="/delete-oldest" method="POST">
        <button type="submit">Remove Oldest Task</button>
      </form>
    </div>
  </div>

  <div class="post-box">
    <h2># สร้างโพสต์ของคุณเลยที่นี่</h2>
    <button class="btn2 ">สร้างโพสต์</button>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
